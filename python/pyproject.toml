[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "steering-functions"
version = "0.1.0"
description = "Pure-Python steering-functions port"
requires-python = ">=3.9"

[tool.setuptools]
packages = ["steering_functions"]
